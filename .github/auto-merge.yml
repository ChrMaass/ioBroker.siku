# Configure here which dependency updates should be merged automatically.
# This repository intentionally keeps auto-merges conservative:
# - production dependencies: only patch updates and security-driven minor bumps
# - development dependencies: patch and minor updates
# - majors stay manual and are reviewed in dedicated PRs
- match:
    dependency_type: production
    update_type: "semver:patch"
- match:
    dependency_type: production
    update_type: "security:minor"
- match:
    dependency_type: development
    update_type: "semver:patch"
- match:
    dependency_type: development
    update_type: "semver:minor"

# The syntax is based on the legacy dependabot v1 automerged_updates syntax, see:
# https://dependabot.com/docs/config-file/#automerged_updates
